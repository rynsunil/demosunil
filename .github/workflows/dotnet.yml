name: .NET core

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: [ self-hosted ]

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 2.0.0
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --configuration Release --no-restore
    - name: Create the package
      run: dotnet pack --configuration Release
    - name: Publish the package 
      run: dotnet nuget push /home/azureuser/actions-runner/_work/test6/test6/hello-world-api/bin/Release/hello-world-api.1.0.0.nupkg
    
    
    
   
